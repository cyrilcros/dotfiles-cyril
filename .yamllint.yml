extends: default

rules:
  truthy:
    # GitHub Actions uses "on" as a key — not a YAML boolean
    ignore: |
      .github/workflows/
  line-length:
    max: 120
  document-start:
    present: false
